@extends('layouts.staff')
@section('title', 'Edit '.$variant->label)
@section('content')
@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif
@if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<div class="mb-4"><a href="{{ route('staff.catalogue.index') }}" class="text-success text-decoration-none">← Back to catalogue</a><h1 class="h2 fw-bold mt-2 mb-0">Edit phone variant</h1><p class="text-muted">{{ $variant->sku }} · {{ $variant->product->brand->name }}</p></div>
<form method="POST" enctype="multipart/form-data" action="{{ route('staff.catalogue.update',$variant) }}">@csrf @method('PUT')<div class="row g-4"><div class="col-xl-8"><section class="staff-card p-4"><div class="row g-3"><div class="col-12"><label class="form-label fw-semibold">Display name</label><input name="label" value="{{ old('label',$variant->label) }}" class="form-control" required></div><div class="col-md-6"><label class="form-label fw-semibold">Price (KES)</label><input type="number" name="price" value="{{ old('price',$variant->price ?: '') }}" class="form-control"></div><div class="col-md-6"><label class="form-label fw-semibold">Condition</label><select name="condition" class="form-select">@foreach(['new','refurbished','used'] as $condition)<option value="{{ $condition }}" @selected(old('condition',$variant->condition)===$condition)>{{ ucfirst($condition) }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Storage GB</label><input type="number" name="storage_gb" value="{{ old('storage_gb',$variant->storage_gb) }}" class="form-control"></div><div class="col-md-3"><label class="form-label">RAM GB</label><input type="number" name="ram_gb" value="{{ old('ram_gb',$variant->ram_gb) }}" class="form-control"></div><div class="col-md-3"><label class="form-label">Colour</label><input name="colour" value="{{ old('colour',$variant->colour) }}" class="form-control"></div><div class="col-md-3"><label class="form-label">Connectivity</label><input name="connectivity" value="{{ old('connectivity',$variant->connectivity) }}" class="form-control"></div><div class="col-12"><label class="form-label fw-semibold">Replace product image</label><input type="file" name="image" accept="image/jpeg,image/png,image/webp" class="form-control"><small class="text-muted">JPG, PNG or WebP, maximum 5MB.</small></div></div></section></div><div class="col-xl-4"><section class="staff-card p-4"><h2 class="h5 fw-bold">Visibility and sales</h2>@foreach([['quote_required','Price on request',$variant->quote_required],['payment_plan_eligible','Payment plan eligible',$variant->payment_plan_eligible],['is_active','Visible on website',$variant->is_active]] as [$name,$label,$checked])<label class="form-check form-switch py-2"><input type="hidden" name="{{ $name }}" value="0"><input class="form-check-input" type="checkbox" name="{{ $name }}" value="1" @checked(old($name,$checked))><span class="form-check-label">{{ $label }}</span></label>@endforeach<button class="btn btn-staff w-100 py-3 mt-3">Save catalogue changes</button></section></div></div></form>
@endsection
