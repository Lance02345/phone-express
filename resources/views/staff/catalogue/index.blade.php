@extends('layouts.staff')
@section('title', 'Manage catalogue')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><span class="text-success small fw-bold">CATALOGUE</span><h1 class="h2 fw-bold mb-0">Manage phone details</h1><p class="text-muted mb-0">Updates appear on the public site immediately.</p></div><form method="GET" class="d-flex gap-2"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Search name or SKU"><button class="btn btn-staff">Search</button></form></div>
<section class="staff-card overflow-hidden"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Phone</th><th>SKU</th><th>Price</th><th>Status</th><th></th></tr></thead><tbody>@foreach($variants as $variant)<tr><td><strong>{{ $variant->label }}</strong><small class="d-block text-muted">{{ $variant->product->brand->name }}</small></td><td><code>{{ $variant->sku }}</code></td><td>{{ $variant->price ? 'KES '.number_format($variant->price) : 'On request' }}</td><td><span class="badge {{ $variant->is_active ? 'bg-success' : 'bg-secondary' }}">{{ $variant->is_active ? 'Active' : 'Hidden' }}</span></td><td class="text-end"><a href="{{ route('staff.catalogue.edit',$variant) }}" class="btn btn-sm btn-outline-success">Edit</a></td></tr>@endforeach</tbody></table></div></section><div class="mt-4">{{ $variants->links('pagination::bootstrap-5') }}</div>
@endsection
